import { TranslationProvider } from "./localization/i18n";
import theme, { extendTheme, useDopebase, DopebaseProvider, DopebaseContext } from "./theming";
import { useTranslations } from './hooks/useTranslations';

export {
  TranslationProvider,
  theme,
  extendTheme,
  useDopebase,
  DopebaseProvider,
  DopebaseContext,
  useTranslations,
};